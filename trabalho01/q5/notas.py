def media(lista_de_notas):

    total = sum(lista_de_notas)
    quantidade = len(lista_de_notas)
    return total / quantidade


def maior(lista_de_notas):
    return max(lista_de_notas)


def menor(lista_de_notas):
    return min(lista_de_notas)
